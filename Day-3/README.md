# Day 03 – Functions & Parameters 🧩

## 📌 Focus
Understanding how JavaScript functions behave with default parameters,
rest parameters, and side effects.

---

## 🧠 Topics Covered
- Default Parameters
- `undefined` vs `null`
- Rest Parameters (`...`)
- Pure vs Impure Functions
- Side Effects in Functions

---

## ✅ Tasks

### 🔹 Task 1: Default Parameters – Edge Cases
- Tested how default parameters behave with missing values
- Compared `undefined` and `null` behavior

**Key Learnings:**
- Default parameters apply only when the argument is `undefined`
- `null` is treated as an explicit value and overrides defaults

---

### 🔹 Task 2: Rest Parameters
- Created a function that accepts unlimited arguments
- Used rest parameters instead of the `arguments` object

**Key Learnings:**
- Rest parameters provide cleaner and more predictable code
- Modern JavaScript avoids the `arguments` object

---

### 🔹 Task 3: Pure vs Impure Functions
- Identified side effects caused by external variables
- Refactored an impure function into a pure function

**Key Learnings:**
- Pure functions return consistent output for the same input
- Avoiding side effects makes code easier to test and debug

---

## 📂 Files
- `task-1.js` → Default parameters behavior
- `task-2.js` → Rest parameters implementation
- `task-3.js` → Pure vs impure function example

---

## 🎯 Outcome
- Deeper understanding of function execution in JavaScript
- Ability to identify and fix side-effect-related bugs
- Strong foundation for closures and advanced patterns

---

